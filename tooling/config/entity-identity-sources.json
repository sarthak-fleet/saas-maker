{
  "$comment": "Where each surface declares its own identity, for tooling/scripts/entity-identity-diff.mjs (SAR-9). The 'docs' source is the product's own public site — the JSON-LD/meta a model actually reads — plus the docs site's deployed URL, assembled from the two blume.config.ts keys that make it up. Paths are relative to the fleet root. Read-only: nothing here is used to write.",
  "surfaces": [
    {
      "id": "codevetter",
      "label": "CodeVetter",
      "domain": "codevetter.com",
      "repoPath": "codevetter",
      "claim": {
        "anchors": ["verification"]
      },
      "docs": {
        "name": {
          "files": ["apps/landing-page-astro/src/layouts/Layout.astro"],
          "patterns": ["\"@type\":\"WebSite\",\"@id\":\"[^\"]*\",\"name\":\"([^\"]+)\""]
        },
        "description": {
          "files": ["apps/landing-page-astro/src/layouts/Layout.astro"],
          "patterns": ["\"@type\":\"WebSite\"[\\s\\S]{0,400}?\"description\":\"([^\"]+)\""]
        },
        "canonicalUrl": {
          "files": ["apps/landing-page-astro/src/layouts/Layout.astro"],
          "patterns": ["const SITE_URL = '([^']+)'"]
        },
        "docsUrl": {
          "kind": "concat",
          "parts": [
            {
              "files": ["docs-site/blume.config.ts"],
              "patterns": ["site: '([^']+)'"]
            },
            {
              "files": ["docs-site/blume.config.ts"],
              "patterns": ["base: '([^']+)'"]
            }
          ]
        },
        "repoUrl": {
          "files": ["apps/landing-page-astro/src/layouts/Layout.astro"],
          "patterns": ["\"sameAs\":\\[\"(https://github\\.com/[^\"/]+/[^\"]+)\""]
        }
      }
    },
    {
      "id": "posttrainllm",
      "label": "PostTrainLLM",
      "domain": "posttrainllm.com",
      "repoPath": "posttrainllm",
      "claim": {
        "anchors": ["factory"]
      },
      "docs": {
        "name": {
          "files": ["browser/src/layouts/Default.astro"],
          "patterns": ["\"@type\":\\s*\"WebSite\"[\\s\\S]{0,200}?\"name\":\\s*\"([^\"]+)\""]
        },
        "description": {
          "files": ["browser/src/layouts/Default.astro"],
          "patterns": ["\"@type\":\\s*\"WebSite\"[\\s\\S]{0,600}?\"description\":\\s*\"([^\"]+)\""]
        },
        "canonicalUrl": {
          "files": ["browser/astro.config.mjs"],
          "patterns": ["site:\\s*\"([^\"]+)\""]
        },
        "docsUrl": {
          "kind": "concat",
          "parts": [
            {
              "files": ["docs-site/blume.config.ts"],
              "patterns": ["site: '([^']+)'"]
            },
            {
              "files": ["docs-site/blume.config.ts"],
              "patterns": ["base: '([^']+)'"]
            }
          ]
        },
        "repoUrl": {
          "files": ["browser/src/layouts/Default.astro"],
          "patterns": ["\"sameAs\":\\s*\\[\"(https://github\\.com/[^\"/]+/[^\"]+)\""]
        }
      }
    },
    {
      "id": "saas-maker",
      "label": "SaaS Maker",
      "domain": "sassmaker.com",
      "repoPath": "saas-maker",
      "claim": {
        "$comment": "'product' rather than 'directory': the registry frames SaaS Maker as a studio that builds focused products, the repo and docs frame it as the public directory of them. That is a positioning difference, and SAR-9's boundary forbids resolving positioning here — flag it, do not fix it. The anchor tests the one claim all three do make.",
        "anchors": ["product"]
      },
      "docs": {
        "name": {
          "files": ["apps/showcase/src/data/studio.ts"],
          "patterns": ["export const STUDIO_PROFILE[\\s\\S]{0,80}?name: '([^']+)'"]
        },
        "description": {
          "files": ["apps/showcase/src/data/studio.ts"],
          "patterns": ["oneLine:\\s*\n?\\s*'([^']+)'"]
        },
        "canonicalUrl": {
          "files": ["apps/showcase/astro.config.mjs"],
          "patterns": ["site: '([^']+)'"]
        },
        "$comment-repoUrl": "sassmaker.com declares sameAs: ['https://github.com/sass-maker'] — the GitHub *org* profile, which is correct for an Organization node and is not a repo URL. So the site declares no repo URL; requiring two path segments makes that absence visible rather than reporting a false conflict.",
        "repoUrl": {
          "files": ["apps/showcase/src/data/studio.ts"],
          "patterns": ["sameAs: \\['(https://github\\.com/[^'/]+/[^']+)'\\]"]
        }
      }
    },
    {
      "id": "high-signal",
      "label": "High Signal",
      "domain": "highsignal.app",
      "repoPath": "high-signal",
      "claim": {
        "anchors": ["daily"]
      },
      "docs": {
        "name": {
          "files": ["apps/web/src/app/layout.tsx"],
          "patterns": ["\"@type\":\"WebSite\",\"@id\":\"[^\"]*\",\"name\":\"([^\"]+)\""]
        },
        "description": {
          "files": ["apps/web/src/app/layout.tsx"],
          "patterns": ["\"@type\":\"WebSite\"[\\s\\S]{0,600}?\"description\":\"([^\"]+)\""]
        },
        "canonicalUrl": {
          "files": ["apps/web/src/app/layout.tsx"],
          "patterns": ["\"@type\":\"WebSite\"[\\s\\S]{0,300}?\"url\":\"([^\"]+)\""]
        },
        "docsUrl": {
          "kind": "concat",
          "parts": [
            {
              "files": ["docs-site/blume.config.ts"],
              "patterns": ["site: '([^']+)'"]
            },
            {
              "files": ["docs-site/blume.config.ts"],
              "patterns": ["base: '([^']+)'"]
            }
          ]
        },
        "repoUrl": {
          "files": ["apps/web/src/app/layout.tsx"],
          "patterns": ["\"sameAs\":\\[\"(https://github\\.com/[^\"/]+/[^\"]+)\""]
        }
      }
    },
    {
      "id": "on-record",
      "label": "High Signal Podcasts",
      "domain": "podcasts.highsignal.app",
      "repoSlug": "Significant-Hobbies/on-record",
      "repoPath": "on-record",
      "claim": {
        "anchors": ["podcast", "episode"],
        "mentions": ["High Signal"]
      },
      "docs": {
        "name": {
          "files": ["apps/web/src/layouts/Base.astro"],
          "patterns": ["'@type': 'WebSite',[\\s\\S]{0,200}?name: '([^']+)'"]
        },
        "description": {
          "files": ["apps/web/src/layouts/Base.astro"],
          "patterns": ["description = '([^']+)'"]
        },
        "canonicalUrl": {
          "files": ["apps/web/src/layouts/Base.astro"],
          "patterns": ["const siteUrl = '([^']+)'"]
        },
        "repoUrl": {
          "files": ["apps/web/src/layouts/Base.astro"],
          "patterns": ["sameAs: \\['(https://github\\.com/[^'/]+/[^']+)'\\]"]
        }
      }
    },
    {
      "id": "significanthobbies",
      "label": "Significant Hobbies",
      "domain": "significanthobbies.com",
      "repoPath": "significanthobbies",
      "$comment": "CAUTION: the 'docs' channel below reads repo source, and for this one surface repo source is NOT a proxy for the deployed site. origin/main's layout.tsx declares a WebSite node at @id .../#app carrying Live's one-liner; the deployed apex declares @id .../#website with the correct hub description, and neither the title nor the meta description match either. Verified 2026-09-05. So a category-drift finding on this surface's `docs` value is a finding about the repo, not about what the public sees — the canonical record's authorityOrder puts the deployed surface first. Tracked in Significant-Hobbies/significanthobbies#148 along with the two apex machine channels (/api/ai 308s to Live's catalog, /llms.txt is Live's file) that ARE wrong in public.",
      "claim": {
        "$comment": "This surface is a hub, so it legitimately names the products it hosts. Without the allowlist the foreign-entity check would fire six times on a description that is correct.",
        "anchors": ["hub", "control plane"],
        "mentions": ["Live", "Journal", "Calorie", "Setline", "Kith", "Anchor"]
      },
      "docs": {
        "name": {
          "files": ["src/app/layout.tsx"],
          "patterns": ["\"@type\":\"WebSite\",\"@id\":\"[^\"]*\",\"name\":\"([^\"]+)\""]
        },
        "description": {
          "files": ["src/app/layout.tsx"],
          "patterns": ["\"@type\":\"WebSite\"[\\s\\S]{0,600}?\"description\":\"([^\"]+)\""]
        },
        "canonicalUrl": {
          "files": ["src/app/layout.tsx"],
          "patterns": ["\"@type\":\"WebSite\"[\\s\\S]{0,300}?\"url\":\"([^\"]+)\""]
        },
        "repoUrl": {
          "files": ["src/app/layout.tsx"],
          "patterns": ["\"sameAs\":\\[\"(https://github\\.com/[^\"/]+/[^\"]+)\"\\]"]
        }
      }
    },
    {
      "id": "what-it-takes-to-win",
      "label": "Look Sideways",
      "domain": "paths.significanthobbies.com",
      "repoPath": "what-it-takes-to-win",
      "claim": {
        "anchors": ["path"]
      },
      "docs": {
        "name": {
          "files": ["src/layouts/Base.astro"],
          "patterns": ["title = \"([^\"]+)\""]
        },
        "description": {
          "files": ["src/scripts/generate_discovery_surfaces.mjs"],
          "patterns": ["\"(Look Sideways is a guided argument[^\"]+)\""]
        },
        "canonicalUrl": {
          "files": ["astro.config.mjs"],
          "patterns": ["site: \"([^\"]+)\""]
        }
      }
    }
  ]
}
