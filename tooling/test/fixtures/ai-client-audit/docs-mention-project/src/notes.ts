// Historically this shipped pointed at https://api.openai.com/v1 before the
// gateway existed. Left as a note; the code below calls nothing.
/*
 * Older revisions also supported https://api.mistral.ai/v1 directly.
 */
export const version = 3;
