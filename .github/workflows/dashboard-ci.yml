name: Dashboard CI

on:
  workflow_call:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: |
            foundry/apps/dashboard/web/pnpm-lock.yaml
            foundry/apps/dashboard/backend/packages/ai-visibility/pnpm-lock.yaml
      - name: Install Dashboard web dependencies
        working-directory: foundry/apps/dashboard/web
        run: pnpm install --frozen-lockfile
      - name: Install AI Awareness package dependencies
        working-directory: foundry/apps/dashboard/backend/packages/ai-visibility
        run: pnpm install --frozen-lockfile
      - run: pnpm run check
